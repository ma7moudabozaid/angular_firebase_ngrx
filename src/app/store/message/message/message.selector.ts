// import { createFeatureSelector, createSelector } from '@ngrx/store';
// import { MessageAppState } from '../app.state';
// import { MessageState } from './message.reducer';

// // import { MessageState } from './message.state';

// // export const messageSelector = (state: MessageState) => state.message;

// // export const selectMessage = createFeatureSelector<Message[]>('messages');



// const messageSelector = (state: MessageAppState) => state.message;

// export const selectMessages = createSelector(
//   messageSelector,(state: MessageState) => state.messages
// );
